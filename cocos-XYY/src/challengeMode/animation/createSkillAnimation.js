function createAnimationSkillXiagurouchang(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_xiagurouchang"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillMengshe(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_mengshe"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillNvwa(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_nvwa"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillGuilingjing(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_guilingjing"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillJieruchou(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_jieruchou"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillZuixianwangyuebu(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_zuixianwangyuebu"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillJujue(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_jujue"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillYuanlingguixinshu(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_yuanlingguixinshu"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillHaosheng(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_haosheng"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillGuanai(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_guanai"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillJuedou(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<39;i++){
		var frameName="skillanimation_juedou"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillZhanbu(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_zhanbu"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillManheng(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_manheng"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillXiongdi(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<31;i++){
		var frameName="skillanimation_xiongdi"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillTaotie(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_taotie"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillHouyisherigong(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_houyisherigong"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillSounangtanbao(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_sounangtanbao"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillMengkuilei(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl,player.skillPng );
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_mengkuilei"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillNingbingfenyan(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_ningbingfenyan"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillZengjian(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_zengjian"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillYuexingzhishu(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_yuexingzhishu"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillWufawutian(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<26;i++){
		var frameName="skillanimation_wufawutian"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillKuanglongxunyingzhan(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<35;i++){
		var frameName="skillanimation_kuanglongxunyingzhan"+fix(i, 2)+".png";
		cc.log(frameName);
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillNitianzhen(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<33;i++){
		var frameName="skillanimation_nitianzhen"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillLitianji(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<33;i++){
		var frameName="skillanimation_litianji"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillDipai(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<29;i++){
		var frameName="skillanimation_dipai"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillJianling(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_jianling"+fix(i, 2)+".png";
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillJianhun(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_jianhun"+fix(i, 2)+".png";
		cc.log(frameName);
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillRumengdiao(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=0;i<27;i++){
		var frameName="skillanimation_rumengdiao"+fix(i, 2)+".png";
		cc.log(frameName);
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}
function createAnimationSkillHuihunxianmeng(player){
	var framCache=cc.spriteFrameCache;
	framCache.addSpriteFrames(player.skillUrl, player.skillPng);
	var animation=new cc.Animation();
	for(var i=1;i<21;i++){
		var frameName="skillanimation_huihunxianmeng"+fix(i, 2)+".png";
		cc.log(frameName);
		var spriteFrame=cc.spriteFrameCache.getSpriteFrame(frameName);
		animation.addSpriteFrame(spriteFrame);
	}
	return animation;
}